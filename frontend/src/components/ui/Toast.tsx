import { ToastContainer } from "../ToastContainer";

export default function Toast() {
  // Este componente ahora solo retorna el ToastContainer
  // que maneja múltiples toasts con la nueva estructura
  return <ToastContainer />;
}
